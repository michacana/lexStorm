/*
 * This source file was generated by FireStorm/DAO.
 * 
 * If you purchase a full license for FireStorm/DAO you can customize this header file.
 * 
 * For more information please visit http://www.codefutures.com/products/firestorm
 */

package com.Abogados.Lex.jdbc;

import com.Abogados.Lex.dao.*;
import com.Abogados.Lex.factory.*;
import com.Abogados.Lex.dto.*;
import com.Abogados.Lex.exceptions.*;
import java.sql.Connection;
import java.util.Collection;
import java.sql.PreparedStatement;
import java.sql.Statement;
import java.sql.ResultSet;
import java.sql.SQLException;
import java.sql.Time;
import java.util.List;
import java.util.Iterator;
import java.util.ArrayList;

public class FormaPagoDaoImpl extends AbstractDAO implements FormaPagoDao
{
	/** 
	 * The factory class for this DAO has two versions of the create() method - one that
takes no arguments and one that takes a Connection argument. If the Connection version
is chosen then the connection will be stored in this attribute and will be used by all
calls to this DAO, otherwise a new Connection will be allocated for each operation.
	 */
	protected java.sql.Connection userConn;

	/** 
	 * All finder methods in this class use this SELECT constant to build their queries
	 */
	protected final String SQL_SELECT = "SELECT ID_FORMA_PAGO, NOMBRE, CANTIDAD_CUOTAS FROM " + getTableName() + "";

	/** 
	 * Finder methods will pass this value to the JDBC setMaxRows method
	 */
	protected int maxRows;

	/** 
	 * SQL INSERT statement for this table
	 */
	protected final String SQL_INSERT = "INSERT INTO " + getTableName() + " ( ID_FORMA_PAGO, NOMBRE, CANTIDAD_CUOTAS ) VALUES ( ?, ?, ? )";

	/** 
	 * SQL UPDATE statement for this table
	 */
	protected final String SQL_UPDATE = "UPDATE " + getTableName() + " SET ID_FORMA_PAGO = ?, NOMBRE = ?, CANTIDAD_CUOTAS = ? WHERE ID_FORMA_PAGO = ?";

	/** 
	 * SQL DELETE statement for this table
	 */
	protected final String SQL_DELETE = "DELETE FROM " + getTableName() + " WHERE ID_FORMA_PAGO = ?";

	/** 
	 * Index of column ID_FORMA_PAGO
	 */
	protected static final int COLUMN_ID_FORMA_PAGO = 1;

	/** 
	 * Index of column NOMBRE
	 */
	protected static final int COLUMN_NOMBRE = 2;

	/** 
	 * Index of column CANTIDAD_CUOTAS
	 */
	protected static final int COLUMN_CANTIDAD_CUOTAS = 3;

	/** 
	 * Number of columns
	 */
	protected static final int NUMBER_OF_COLUMNS = 3;

	/** 
	 * Index of primary-key column ID_FORMA_PAGO
	 */
	protected static final int PK_COLUMN_ID_FORMA_PAGO = 1;

	/** 
	 * Inserts a new row in the FORMA_PAGO table.
	 */
	public FormaPagoPk insert(FormaPago dto) throws FormaPagoDaoException
	{
		long t1 = System.currentTimeMillis();
		// declare variables
		final boolean isConnSupplied = (userConn != null);
		Connection conn = null;
		PreparedStatement stmt = null;
		ResultSet rs = null;
		
		try {
			// get the user-specified connection or get a connection from the ResourceManager
			conn = isConnSupplied ? userConn : ResourceManager.getConnection();
		
			stmt = conn.prepareStatement( SQL_INSERT );
			int index = 1;
			stmt.setLong( index++, dto.getIdFormaPago() );
			stmt.setString( index++, dto.getNombre() );
			stmt.setInt( index++, dto.getCantidadCuotas() );
			System.out.println( "Executing " + SQL_INSERT + " with DTO: " + dto );
			int rows = stmt.executeUpdate();
			long t2 = System.currentTimeMillis();
			System.out.println( rows + " rows affected (" + (t2-t1) + " ms)" );
			reset(dto);
			return dto.createPk();
		}
		catch (Exception _e) {
			_e.printStackTrace();
			throw new FormaPagoDaoException( "Exception: " + _e.getMessage(), _e );
		}
		finally {
			ResourceManager.close(stmt);
			if (!isConnSupplied) {
				ResourceManager.close(conn);
			}
		
		}
		
	}

	/** 
	 * Updates a single row in the FORMA_PAGO table.
	 */
	public void update(FormaPagoPk pk, FormaPago dto) throws FormaPagoDaoException
	{
		long t1 = System.currentTimeMillis();
		// declare variables
		final boolean isConnSupplied = (userConn != null);
		Connection conn = null;
		PreparedStatement stmt = null;
		
		try {
			// get the user-specified connection or get a connection from the ResourceManager
			conn = isConnSupplied ? userConn : ResourceManager.getConnection();
		
			System.out.println( "Executing " + SQL_UPDATE + " with DTO: " + dto );
			stmt = conn.prepareStatement( SQL_UPDATE );
			int index=1;
			stmt.setLong( index++, dto.getIdFormaPago() );
			stmt.setString( index++, dto.getNombre() );
			stmt.setInt( index++, dto.getCantidadCuotas() );
			stmt.setLong( 4, pk.getIdFormaPago() );
			int rows = stmt.executeUpdate();
			reset(dto);
			long t2 = System.currentTimeMillis();
			System.out.println( rows + " rows affected (" + (t2-t1) + " ms)" );
		}
		catch (Exception _e) {
			_e.printStackTrace();
			throw new FormaPagoDaoException( "Exception: " + _e.getMessage(), _e );
		}
		finally {
			ResourceManager.close(stmt);
			if (!isConnSupplied) {
				ResourceManager.close(conn);
			}
		
		}
		
	}

	/** 
	 * Deletes a single row in the FORMA_PAGO table.
	 */
	public void delete(FormaPagoPk pk) throws FormaPagoDaoException
	{
		long t1 = System.currentTimeMillis();
		// declare variables
		final boolean isConnSupplied = (userConn != null);
		Connection conn = null;
		PreparedStatement stmt = null;
		
		try {
			// get the user-specified connection or get a connection from the ResourceManager
			conn = isConnSupplied ? userConn : ResourceManager.getConnection();
		
			System.out.println( "Executing " + SQL_DELETE + " with PK: " + pk );
			stmt = conn.prepareStatement( SQL_DELETE );
			stmt.setLong( 1, pk.getIdFormaPago() );
			int rows = stmt.executeUpdate();
			long t2 = System.currentTimeMillis();
			System.out.println( rows + " rows affected (" + (t2-t1) + " ms)" );
		}
		catch (Exception _e) {
			_e.printStackTrace();
			throw new FormaPagoDaoException( "Exception: " + _e.getMessage(), _e );
		}
		finally {
			ResourceManager.close(stmt);
			if (!isConnSupplied) {
				ResourceManager.close(conn);
			}
		
		}
		
	}

	/** 
	 * Returns the rows from the FORMA_PAGO table that matches the specified primary-key value.
	 */
	public FormaPago findByPrimaryKey(FormaPagoPk pk) throws FormaPagoDaoException
	{
		return findByPrimaryKey( pk.getIdFormaPago() );
	}

	/** 
	 * Returns all rows from the FORMA_PAGO table that match the criteria 'ID_FORMA_PAGO = :idFormaPago'.
	 */
	public FormaPago findByPrimaryKey(long idFormaPago) throws FormaPagoDaoException
	{
		FormaPago ret[] = findByDynamicSelect( SQL_SELECT + " WHERE ID_FORMA_PAGO = ?", new Object[] {  new Long(idFormaPago) } );
		return ret.length==0 ? null : ret[0];
	}

	/** 
	 * Returns all rows from the FORMA_PAGO table that match the criteria ''.
	 */
	public FormaPago[] findAll() throws FormaPagoDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " ORDER BY ID_FORMA_PAGO", null );
	}

	/** 
	 * Returns all rows from the FORMA_PAGO table that match the criteria 'ID_FORMA_PAGO = :idFormaPago'.
	 */
	public FormaPago[] findWhereIdFormaPagoEquals(long idFormaPago) throws FormaPagoDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " WHERE ID_FORMA_PAGO = ? ORDER BY ID_FORMA_PAGO", new Object[] {  new Long(idFormaPago) } );
	}

	/** 
	 * Returns all rows from the FORMA_PAGO table that match the criteria 'NOMBRE = :nombre'.
	 */
	public FormaPago[] findWhereNombreEquals(String nombre) throws FormaPagoDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " WHERE NOMBRE = ? ORDER BY NOMBRE", new Object[] { nombre } );
	}

	/** 
	 * Returns all rows from the FORMA_PAGO table that match the criteria 'CANTIDAD_CUOTAS = :cantidadCuotas'.
	 */
	public FormaPago[] findWhereCantidadCuotasEquals(int cantidadCuotas) throws FormaPagoDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " WHERE CANTIDAD_CUOTAS = ? ORDER BY CANTIDAD_CUOTAS", new Object[] {  new Integer(cantidadCuotas) } );
	}

	/**
	 * Method 'FormaPagoDaoImpl'
	 * 
	 */
	public FormaPagoDaoImpl()
	{
	}

	/**
	 * Method 'FormaPagoDaoImpl'
	 * 
	 * @param userConn
	 */
	public FormaPagoDaoImpl(final java.sql.Connection userConn)
	{
		this.userConn = userConn;
	}

	/** 
	 * Sets the value of maxRows
	 */
	public void setMaxRows(int maxRows)
	{
		this.maxRows = maxRows;
	}

	/** 
	 * Gets the value of maxRows
	 */
	public int getMaxRows()
	{
		return maxRows;
	}

	/**
	 * Method 'getTableName'
	 * 
	 * @return String
	 */
	public String getTableName()
	{
		return "LEX.FORMA_PAGO";
	}

	/** 
	 * Fetches a single row from the result set
	 */
	protected FormaPago fetchSingleResult(ResultSet rs) throws SQLException
	{
		if (rs.next()) {
			FormaPago dto = new FormaPago();
			populateDto( dto, rs);
			return dto;
		} else {
			return null;
		}
		
	}

	/** 
	 * Fetches multiple rows from the result set
	 */
	protected FormaPago[] fetchMultiResults(ResultSet rs) throws SQLException
	{
		Collection resultList = new ArrayList();
		while (rs.next()) {
			FormaPago dto = new FormaPago();
			populateDto( dto, rs);
			resultList.add( dto );
		}
		
		FormaPago ret[] = new FormaPago[ resultList.size() ];
		resultList.toArray( ret );
		return ret;
	}

	/** 
	 * Populates a DTO with data from a ResultSet
	 */
	protected void populateDto(FormaPago dto, ResultSet rs) throws SQLException
	{
		dto.setIdFormaPago( rs.getLong( COLUMN_ID_FORMA_PAGO ) );
		dto.setNombre( rs.getString( COLUMN_NOMBRE ) );
		dto.setCantidadCuotas( rs.getInt( COLUMN_CANTIDAD_CUOTAS ) );
	}

	/** 
	 * Resets the modified attributes in the DTO
	 */
	protected void reset(FormaPago dto)
	{
	}

	/** 
	 * Returns all rows from the FORMA_PAGO table that match the specified arbitrary SQL statement
	 */
	public FormaPago[] findByDynamicSelect(String sql, Object[] sqlParams) throws FormaPagoDaoException
	{
		// declare variables
		final boolean isConnSupplied = (userConn != null);
		Connection conn = null;
		PreparedStatement stmt = null;
		ResultSet rs = null;
		
		try {
			// get the user-specified connection or get a connection from the ResourceManager
			conn = isConnSupplied ? userConn : ResourceManager.getConnection();
		
			// construct the SQL statement
			final String SQL = sql;
		
		
			System.out.println( "Executing " + SQL );
			// prepare statement
			stmt = conn.prepareStatement( SQL );
			stmt.setMaxRows( maxRows );
		
			// bind parameters
			for (int i=0; sqlParams!=null && i<sqlParams.length; i++ ) {
				stmt.setObject( i+1, sqlParams[i] );
			}
		
		
			rs = stmt.executeQuery();
		
			// fetch the results
			return fetchMultiResults(rs);
		}
		catch (Exception _e) {
			_e.printStackTrace();
			throw new FormaPagoDaoException( "Exception: " + _e.getMessage(), _e );
		}
		finally {
			ResourceManager.close(rs);
			ResourceManager.close(stmt);
			if (!isConnSupplied) {
				ResourceManager.close(conn);
			}
		
		}
		
	}

	/** 
	 * Returns all rows from the FORMA_PAGO table that match the specified arbitrary SQL statement
	 */
	public FormaPago[] findByDynamicWhere(String sql, Object[] sqlParams) throws FormaPagoDaoException
	{
		// declare variables
		final boolean isConnSupplied = (userConn != null);
		Connection conn = null;
		PreparedStatement stmt = null;
		ResultSet rs = null;
		
		try {
			// get the user-specified connection or get a connection from the ResourceManager
			conn = isConnSupplied ? userConn : ResourceManager.getConnection();
		
			// construct the SQL statement
			final String SQL = SQL_SELECT + " WHERE " + sql;
		
		
			System.out.println( "Executing " + SQL );
			// prepare statement
			stmt = conn.prepareStatement( SQL );
			stmt.setMaxRows( maxRows );
		
			// bind parameters
			for (int i=0; sqlParams!=null && i<sqlParams.length; i++ ) {
				stmt.setObject( i+1, sqlParams[i] );
			}
		
		
			rs = stmt.executeQuery();
		
			// fetch the results
			return fetchMultiResults(rs);
		}
		catch (Exception _e) {
			_e.printStackTrace();
			throw new FormaPagoDaoException( "Exception: " + _e.getMessage(), _e );
		}
		finally {
			ResourceManager.close(rs);
			ResourceManager.close(stmt);
			if (!isConnSupplied) {
				ResourceManager.close(conn);
			}
		
		}
		
	}

}

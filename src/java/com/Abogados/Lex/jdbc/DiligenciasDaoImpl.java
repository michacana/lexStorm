/*
 * This source file was generated by FireStorm/DAO.
 * 
 * If you purchase a full license for FireStorm/DAO you can customize this header file.
 * 
 * For more information please visit http://www.codefutures.com/products/firestorm
 */

package com.Abogados.Lex.jdbc;

import com.Abogados.Lex.dao.*;
import com.Abogados.Lex.factory.*;
import java.util.Date;
import com.Abogados.Lex.dto.*;
import com.Abogados.Lex.exceptions.*;
import java.sql.Connection;
import java.util.Collection;
import java.sql.PreparedStatement;
import java.sql.Statement;
import java.sql.ResultSet;
import java.sql.SQLException;
import java.sql.Time;
import java.util.List;
import java.util.Iterator;
import java.util.ArrayList;

public class DiligenciasDaoImpl extends AbstractDAO implements DiligenciasDao
{
	/** 
	 * The factory class for this DAO has two versions of the create() method - one that
takes no arguments and one that takes a Connection argument. If the Connection version
is chosen then the connection will be stored in this attribute and will be used by all
calls to this DAO, otherwise a new Connection will be allocated for each operation.
	 */
	protected java.sql.Connection userConn;

	/** 
	 * All finder methods in this class use this SELECT constant to build their queries
	 */
	protected final String SQL_SELECT = "SELECT ID_DILIGENCIA, FECHA_HORA, DESCRIPCION, ID_CAUSA FROM " + getTableName() + "";

	/** 
	 * Finder methods will pass this value to the JDBC setMaxRows method
	 */
	protected int maxRows;

	/** 
	 * SQL INSERT statement for this table
	 */
	protected final String SQL_INSERT = "INSERT INTO " + getTableName() + " ( ID_DILIGENCIA, FECHA_HORA, DESCRIPCION, ID_CAUSA ) VALUES ( ?, ?, ?, ? )";

	/** 
	 * SQL UPDATE statement for this table
	 */
	protected final String SQL_UPDATE = "UPDATE " + getTableName() + " SET ID_DILIGENCIA = ?, FECHA_HORA = ?, DESCRIPCION = ?, ID_CAUSA = ? WHERE ID_DILIGENCIA = ?";

	/** 
	 * SQL DELETE statement for this table
	 */
	protected final String SQL_DELETE = "DELETE FROM " + getTableName() + " WHERE ID_DILIGENCIA = ?";

	/** 
	 * Index of column ID_DILIGENCIA
	 */
	protected static final int COLUMN_ID_DILIGENCIA = 1;

	/** 
	 * Index of column FECHA_HORA
	 */
	protected static final int COLUMN_FECHA_HORA = 2;

	/** 
	 * Index of column DESCRIPCION
	 */
	protected static final int COLUMN_DESCRIPCION = 3;

	/** 
	 * Index of column ID_CAUSA
	 */
	protected static final int COLUMN_ID_CAUSA = 4;

	/** 
	 * Number of columns
	 */
	protected static final int NUMBER_OF_COLUMNS = 4;

	/** 
	 * Index of primary-key column ID_DILIGENCIA
	 */
	protected static final int PK_COLUMN_ID_DILIGENCIA = 1;

	/** 
	 * Inserts a new row in the DILIGENCIAS table.
	 */
	public DiligenciasPk insert(Diligencias dto) throws DiligenciasDaoException
	{
		long t1 = System.currentTimeMillis();
		// declare variables
		final boolean isConnSupplied = (userConn != null);
		Connection conn = null;
		PreparedStatement stmt = null;
		ResultSet rs = null;
		
		try {
			// get the user-specified connection or get a connection from the ResourceManager
			conn = isConnSupplied ? userConn : ResourceManager.getConnection();
		
			stmt = conn.prepareStatement( SQL_INSERT );
			int index = 1;
			stmt.setLong( index++, dto.getIdDiligencia() );
			stmt.setTimestamp(index++, dto.getFechaHora()==null ? null : new java.sql.Timestamp( dto.getFechaHora().getTime() ) );
			stmt.setString( index++, dto.getDescripcion() );
			if (dto.isIdCausaNull()) {
				stmt.setNull( index++, java.sql.Types.INTEGER );
			} else {
				stmt.setLong( index++, dto.getIdCausa() );
			}
		
			System.out.println( "Executing " + SQL_INSERT + " with DTO: " + dto );
			int rows = stmt.executeUpdate();
			long t2 = System.currentTimeMillis();
			System.out.println( rows + " rows affected (" + (t2-t1) + " ms)" );
			reset(dto);
			return dto.createPk();
		}
		catch (Exception _e) {
			_e.printStackTrace();
			throw new DiligenciasDaoException( "Exception: " + _e.getMessage(), _e );
		}
		finally {
			ResourceManager.close(stmt);
			if (!isConnSupplied) {
				ResourceManager.close(conn);
			}
		
		}
		
	}

	/** 
	 * Updates a single row in the DILIGENCIAS table.
	 */
	public void update(DiligenciasPk pk, Diligencias dto) throws DiligenciasDaoException
	{
		long t1 = System.currentTimeMillis();
		// declare variables
		final boolean isConnSupplied = (userConn != null);
		Connection conn = null;
		PreparedStatement stmt = null;
		
		try {
			// get the user-specified connection or get a connection from the ResourceManager
			conn = isConnSupplied ? userConn : ResourceManager.getConnection();
		
			System.out.println( "Executing " + SQL_UPDATE + " with DTO: " + dto );
			stmt = conn.prepareStatement( SQL_UPDATE );
			int index=1;
			stmt.setLong( index++, dto.getIdDiligencia() );
			stmt.setTimestamp(index++, dto.getFechaHora()==null ? null : new java.sql.Timestamp( dto.getFechaHora().getTime() ) );
			stmt.setString( index++, dto.getDescripcion() );
			if (dto.isIdCausaNull()) {
				stmt.setNull( index++, java.sql.Types.INTEGER );
			} else {
				stmt.setLong( index++, dto.getIdCausa() );
			}
		
			stmt.setLong( 5, pk.getIdDiligencia() );
			int rows = stmt.executeUpdate();
			reset(dto);
			long t2 = System.currentTimeMillis();
			System.out.println( rows + " rows affected (" + (t2-t1) + " ms)" );
		}
		catch (Exception _e) {
			_e.printStackTrace();
			throw new DiligenciasDaoException( "Exception: " + _e.getMessage(), _e );
		}
		finally {
			ResourceManager.close(stmt);
			if (!isConnSupplied) {
				ResourceManager.close(conn);
			}
		
		}
		
	}

	/** 
	 * Deletes a single row in the DILIGENCIAS table.
	 */
	public void delete(DiligenciasPk pk) throws DiligenciasDaoException
	{
		long t1 = System.currentTimeMillis();
		// declare variables
		final boolean isConnSupplied = (userConn != null);
		Connection conn = null;
		PreparedStatement stmt = null;
		
		try {
			// get the user-specified connection or get a connection from the ResourceManager
			conn = isConnSupplied ? userConn : ResourceManager.getConnection();
		
			System.out.println( "Executing " + SQL_DELETE + " with PK: " + pk );
			stmt = conn.prepareStatement( SQL_DELETE );
			stmt.setLong( 1, pk.getIdDiligencia() );
			int rows = stmt.executeUpdate();
			long t2 = System.currentTimeMillis();
			System.out.println( rows + " rows affected (" + (t2-t1) + " ms)" );
		}
		catch (Exception _e) {
			_e.printStackTrace();
			throw new DiligenciasDaoException( "Exception: " + _e.getMessage(), _e );
		}
		finally {
			ResourceManager.close(stmt);
			if (!isConnSupplied) {
				ResourceManager.close(conn);
			}
		
		}
		
	}

	/** 
	 * Returns the rows from the DILIGENCIAS table that matches the specified primary-key value.
	 */
	public Diligencias findByPrimaryKey(DiligenciasPk pk) throws DiligenciasDaoException
	{
		return findByPrimaryKey( pk.getIdDiligencia() );
	}

	/** 
	 * Returns all rows from the DILIGENCIAS table that match the criteria 'ID_DILIGENCIA = :idDiligencia'.
	 */
	public Diligencias findByPrimaryKey(long idDiligencia) throws DiligenciasDaoException
	{
		Diligencias ret[] = findByDynamicSelect( SQL_SELECT + " WHERE ID_DILIGENCIA = ?", new Object[] {  new Long(idDiligencia) } );
		return ret.length==0 ? null : ret[0];
	}

	/** 
	 * Returns all rows from the DILIGENCIAS table that match the criteria ''.
	 */
	public Diligencias[] findAll() throws DiligenciasDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " ORDER BY ID_DILIGENCIA", null );
	}

	/** 
	 * Returns all rows from the DILIGENCIAS table that match the criteria 'ID_CAUSA = :idCausa'.
	 */
	public Diligencias[] findByCausas(long idCausa) throws DiligenciasDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " WHERE ID_CAUSA = ?", new Object[] {  new Long(idCausa) } );
	}

	/** 
	 * Returns all rows from the DILIGENCIAS table that match the criteria 'ID_DILIGENCIA = :idDiligencia'.
	 */
	public Diligencias[] findWhereIdDiligenciaEquals(long idDiligencia) throws DiligenciasDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " WHERE ID_DILIGENCIA = ? ORDER BY ID_DILIGENCIA", new Object[] {  new Long(idDiligencia) } );
	}

	/** 
	 * Returns all rows from the DILIGENCIAS table that match the criteria 'FECHA_HORA = :fechaHora'.
	 */
	public Diligencias[] findWhereFechaHoraEquals(Date fechaHora) throws DiligenciasDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " WHERE FECHA_HORA = ? ORDER BY FECHA_HORA", new Object[] { fechaHora==null ? null : new java.sql.Timestamp( fechaHora.getTime() ) } );
	}

	/** 
	 * Returns all rows from the DILIGENCIAS table that match the criteria 'DESCRIPCION = :descripcion'.
	 */
	public Diligencias[] findWhereDescripcionEquals(String descripcion) throws DiligenciasDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " WHERE DESCRIPCION = ? ORDER BY DESCRIPCION", new Object[] { descripcion } );
	}

	/** 
	 * Returns all rows from the DILIGENCIAS table that match the criteria 'ID_CAUSA = :idCausa'.
	 */
	public Diligencias[] findWhereIdCausaEquals(long idCausa) throws DiligenciasDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " WHERE ID_CAUSA = ? ORDER BY ID_CAUSA", new Object[] {  new Long(idCausa) } );
	}

	/**
	 * Method 'DiligenciasDaoImpl'
	 * 
	 */
	public DiligenciasDaoImpl()
	{
	}

	/**
	 * Method 'DiligenciasDaoImpl'
	 * 
	 * @param userConn
	 */
	public DiligenciasDaoImpl(final java.sql.Connection userConn)
	{
		this.userConn = userConn;
	}

	/** 
	 * Sets the value of maxRows
	 */
	public void setMaxRows(int maxRows)
	{
		this.maxRows = maxRows;
	}

	/** 
	 * Gets the value of maxRows
	 */
	public int getMaxRows()
	{
		return maxRows;
	}

	/**
	 * Method 'getTableName'
	 * 
	 * @return String
	 */
	public String getTableName()
	{
		return "LEX.DILIGENCIAS";
	}

	/** 
	 * Fetches a single row from the result set
	 */
	protected Diligencias fetchSingleResult(ResultSet rs) throws SQLException
	{
		if (rs.next()) {
			Diligencias dto = new Diligencias();
			populateDto( dto, rs);
			return dto;
		} else {
			return null;
		}
		
	}

	/** 
	 * Fetches multiple rows from the result set
	 */
	protected Diligencias[] fetchMultiResults(ResultSet rs) throws SQLException
	{
		Collection resultList = new ArrayList();
		while (rs.next()) {
			Diligencias dto = new Diligencias();
			populateDto( dto, rs);
			resultList.add( dto );
		}
		
		Diligencias ret[] = new Diligencias[ resultList.size() ];
		resultList.toArray( ret );
		return ret;
	}

	/** 
	 * Populates a DTO with data from a ResultSet
	 */
	protected void populateDto(Diligencias dto, ResultSet rs) throws SQLException
	{
		dto.setIdDiligencia( rs.getLong( COLUMN_ID_DILIGENCIA ) );
		dto.setFechaHora( rs.getTimestamp(COLUMN_FECHA_HORA ) );
		dto.setDescripcion( rs.getString( COLUMN_DESCRIPCION ) );
		dto.setIdCausa( rs.getLong( COLUMN_ID_CAUSA ) );
		if (rs.wasNull()) {
			dto.setIdCausaNull( true );
		}
		
	}

	/** 
	 * Resets the modified attributes in the DTO
	 */
	protected void reset(Diligencias dto)
	{
	}

	/** 
	 * Returns all rows from the DILIGENCIAS table that match the specified arbitrary SQL statement
	 */
	public Diligencias[] findByDynamicSelect(String sql, Object[] sqlParams) throws DiligenciasDaoException
	{
		// declare variables
		final boolean isConnSupplied = (userConn != null);
		Connection conn = null;
		PreparedStatement stmt = null;
		ResultSet rs = null;
		
		try {
			// get the user-specified connection or get a connection from the ResourceManager
			conn = isConnSupplied ? userConn : ResourceManager.getConnection();
		
			// construct the SQL statement
			final String SQL = sql;
		
		
			System.out.println( "Executing " + SQL );
			// prepare statement
			stmt = conn.prepareStatement( SQL );
			stmt.setMaxRows( maxRows );
		
			// bind parameters
			for (int i=0; sqlParams!=null && i<sqlParams.length; i++ ) {
				stmt.setObject( i+1, sqlParams[i] );
			}
		
		
			rs = stmt.executeQuery();
		
			// fetch the results
			return fetchMultiResults(rs);
		}
		catch (Exception _e) {
			_e.printStackTrace();
			throw new DiligenciasDaoException( "Exception: " + _e.getMessage(), _e );
		}
		finally {
			ResourceManager.close(rs);
			ResourceManager.close(stmt);
			if (!isConnSupplied) {
				ResourceManager.close(conn);
			}
		
		}
		
	}

	/** 
	 * Returns all rows from the DILIGENCIAS table that match the specified arbitrary SQL statement
	 */
	public Diligencias[] findByDynamicWhere(String sql, Object[] sqlParams) throws DiligenciasDaoException
	{
		// declare variables
		final boolean isConnSupplied = (userConn != null);
		Connection conn = null;
		PreparedStatement stmt = null;
		ResultSet rs = null;
		
		try {
			// get the user-specified connection or get a connection from the ResourceManager
			conn = isConnSupplied ? userConn : ResourceManager.getConnection();
		
			// construct the SQL statement
			final String SQL = SQL_SELECT + " WHERE " + sql;
		
		
			System.out.println( "Executing " + SQL );
			// prepare statement
			stmt = conn.prepareStatement( SQL );
			stmt.setMaxRows( maxRows );
		
			// bind parameters
			for (int i=0; sqlParams!=null && i<sqlParams.length; i++ ) {
				stmt.setObject( i+1, sqlParams[i] );
			}
		
		
			rs = stmt.executeQuery();
		
			// fetch the results
			return fetchMultiResults(rs);
		}
		catch (Exception _e) {
			_e.printStackTrace();
			throw new DiligenciasDaoException( "Exception: " + _e.getMessage(), _e );
		}
		finally {
			ResourceManager.close(rs);
			ResourceManager.close(stmt);
			if (!isConnSupplied) {
				ResourceManager.close(conn);
			}
		
		}
		
	}

}

/*
 * This source file was generated by FireStorm/DAO.
 * 
 * If you purchase a full license for FireStorm/DAO you can customize this header file.
 * 
 * For more information please visit http://www.codefutures.com/products/firestorm
 */

package com.Abogados.Lex.jdbc;

import com.Abogados.Lex.dao.*;
import com.Abogados.Lex.factory.*;
import java.util.Date;
import com.Abogados.Lex.dto.*;
import com.Abogados.Lex.exceptions.*;
import java.sql.Connection;
import java.util.Collection;
import java.sql.PreparedStatement;
import java.sql.Statement;
import java.sql.ResultSet;
import java.sql.SQLException;
import java.sql.Time;
import java.util.List;
import java.util.Iterator;
import java.util.ArrayList;

public class CitasDaoImpl extends AbstractDAO implements CitasDao
{
	/** 
	 * The factory class for this DAO has two versions of the create() method - one that
takes no arguments and one that takes a Connection argument. If the Connection version
is chosen then the connection will be stored in this attribute and will be used by all
calls to this DAO, otherwise a new Connection will be allocated for each operation.
	 */
	protected java.sql.Connection userConn;

	/** 
	 * All finder methods in this class use this SELECT constant to build their queries
	 */
	protected final String SQL_SELECT = "SELECT ID_CITA, FECHA_HORA, ID_NOTARIA, ID_ESTADO_CITA FROM " + getTableName() + "";

	/** 
	 * Finder methods will pass this value to the JDBC setMaxRows method
	 */
	protected int maxRows;

	/** 
	 * SQL INSERT statement for this table
	 */
	protected final String SQL_INSERT = "INSERT INTO " + getTableName() + " ( ID_CITA, FECHA_HORA, ID_NOTARIA, ID_ESTADO_CITA ) VALUES ( ?, ?, ?, ? )";

	/** 
	 * SQL UPDATE statement for this table
	 */
	protected final String SQL_UPDATE = "UPDATE " + getTableName() + " SET ID_CITA = ?, FECHA_HORA = ?, ID_NOTARIA = ?, ID_ESTADO_CITA = ? WHERE ID_CITA = ?";

	/** 
	 * SQL DELETE statement for this table
	 */
	protected final String SQL_DELETE = "DELETE FROM " + getTableName() + " WHERE ID_CITA = ?";

	/** 
	 * Index of column ID_CITA
	 */
	protected static final int COLUMN_ID_CITA = 1;

	/** 
	 * Index of column FECHA_HORA
	 */
	protected static final int COLUMN_FECHA_HORA = 2;

	/** 
	 * Index of column ID_NOTARIA
	 */
	protected static final int COLUMN_ID_NOTARIA = 3;

	/** 
	 * Index of column ID_ESTADO_CITA
	 */
	protected static final int COLUMN_ID_ESTADO_CITA = 4;

	/** 
	 * Number of columns
	 */
	protected static final int NUMBER_OF_COLUMNS = 4;

	/** 
	 * Index of primary-key column ID_CITA
	 */
	protected static final int PK_COLUMN_ID_CITA = 1;

	/** 
	 * Inserts a new row in the CITAS table.
	 */
	public CitasPk insert(Citas dto) throws CitasDaoException
	{
		long t1 = System.currentTimeMillis();
		// declare variables
		final boolean isConnSupplied = (userConn != null);
		Connection conn = null;
		PreparedStatement stmt = null;
		ResultSet rs = null;
		
		try {
			// get the user-specified connection or get a connection from the ResourceManager
			conn = isConnSupplied ? userConn : ResourceManager.getConnection();
		
			stmt = conn.prepareStatement( SQL_INSERT );
			int index = 1;
			stmt.setLong( index++, dto.getIdCita() );
			stmt.setTimestamp(index++, dto.getFechaHora()==null ? null : new java.sql.Timestamp( dto.getFechaHora().getTime() ) );
			stmt.setLong( index++, dto.getIdNotaria() );
			stmt.setLong( index++, dto.getIdEstadoCita() );
			System.out.println( "Executing " + SQL_INSERT + " with DTO: " + dto );
			int rows = stmt.executeUpdate();
			long t2 = System.currentTimeMillis();
			System.out.println( rows + " rows affected (" + (t2-t1) + " ms)" );
			reset(dto);
			return dto.createPk();
		}
		catch (Exception _e) {
			_e.printStackTrace();
			throw new CitasDaoException( "Exception: " + _e.getMessage(), _e );
		}
		finally {
			ResourceManager.close(stmt);
			if (!isConnSupplied) {
				ResourceManager.close(conn);
			}
		
		}
		
	}

	/** 
	 * Updates a single row in the CITAS table.
	 */
	public void update(CitasPk pk, Citas dto) throws CitasDaoException
	{
		long t1 = System.currentTimeMillis();
		// declare variables
		final boolean isConnSupplied = (userConn != null);
		Connection conn = null;
		PreparedStatement stmt = null;
		
		try {
			// get the user-specified connection or get a connection from the ResourceManager
			conn = isConnSupplied ? userConn : ResourceManager.getConnection();
		
			System.out.println( "Executing " + SQL_UPDATE + " with DTO: " + dto );
			stmt = conn.prepareStatement( SQL_UPDATE );
			int index=1;
			stmt.setLong( index++, dto.getIdCita() );
			stmt.setTimestamp(index++, dto.getFechaHora()==null ? null : new java.sql.Timestamp( dto.getFechaHora().getTime() ) );
			stmt.setLong( index++, dto.getIdNotaria() );
			stmt.setLong( index++, dto.getIdEstadoCita() );
			stmt.setLong( 5, pk.getIdCita() );
			int rows = stmt.executeUpdate();
			reset(dto);
			long t2 = System.currentTimeMillis();
			System.out.println( rows + " rows affected (" + (t2-t1) + " ms)" );
		}
		catch (Exception _e) {
			_e.printStackTrace();
			throw new CitasDaoException( "Exception: " + _e.getMessage(), _e );
		}
		finally {
			ResourceManager.close(stmt);
			if (!isConnSupplied) {
				ResourceManager.close(conn);
			}
		
		}
		
	}

	/** 
	 * Deletes a single row in the CITAS table.
	 */
	public void delete(CitasPk pk) throws CitasDaoException
	{
		long t1 = System.currentTimeMillis();
		// declare variables
		final boolean isConnSupplied = (userConn != null);
		Connection conn = null;
		PreparedStatement stmt = null;
		
		try {
			// get the user-specified connection or get a connection from the ResourceManager
			conn = isConnSupplied ? userConn : ResourceManager.getConnection();
		
			System.out.println( "Executing " + SQL_DELETE + " with PK: " + pk );
			stmt = conn.prepareStatement( SQL_DELETE );
			stmt.setLong( 1, pk.getIdCita() );
			int rows = stmt.executeUpdate();
			long t2 = System.currentTimeMillis();
			System.out.println( rows + " rows affected (" + (t2-t1) + " ms)" );
		}
		catch (Exception _e) {
			_e.printStackTrace();
			throw new CitasDaoException( "Exception: " + _e.getMessage(), _e );
		}
		finally {
			ResourceManager.close(stmt);
			if (!isConnSupplied) {
				ResourceManager.close(conn);
			}
		
		}
		
	}

	/** 
	 * Returns the rows from the CITAS table that matches the specified primary-key value.
	 */
	public Citas findByPrimaryKey(CitasPk pk) throws CitasDaoException
	{
		return findByPrimaryKey( pk.getIdCita() );
	}

	/** 
	 * Returns all rows from the CITAS table that match the criteria 'ID_CITA = :idCita'.
	 */
	public Citas findByPrimaryKey(long idCita) throws CitasDaoException
	{
		Citas ret[] = findByDynamicSelect( SQL_SELECT + " WHERE ID_CITA = ?", new Object[] {  new Long(idCita) } );
		return ret.length==0 ? null : ret[0];
	}

	/** 
	 * Returns all rows from the CITAS table that match the criteria ''.
	 */
	public Citas[] findAll() throws CitasDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " ORDER BY ID_CITA", null );
	}

	/** 
	 * Returns all rows from the CITAS table that match the criteria 'ID_ESTADO_CITA = :idEstadoCita'.
	 */
	public Citas[] findByCitaEstados(long idEstadoCita) throws CitasDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " WHERE ID_ESTADO_CITA = ?", new Object[] {  new Long(idEstadoCita) } );
	}

	/** 
	 * Returns all rows from the CITAS table that match the criteria 'ID_NOTARIA = :idNotaria'.
	 */
	public Citas[] findByNotarias(long idNotaria) throws CitasDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " WHERE ID_NOTARIA = ?", new Object[] {  new Long(idNotaria) } );
	}

	/** 
	 * Returns all rows from the CITAS table that match the criteria 'ID_CITA = :idCita'.
	 */
	public Citas[] findWhereIdCitaEquals(long idCita) throws CitasDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " WHERE ID_CITA = ? ORDER BY ID_CITA", new Object[] {  new Long(idCita) } );
	}

	/** 
	 * Returns all rows from the CITAS table that match the criteria 'FECHA_HORA = :fechaHora'.
	 */
	public Citas[] findWhereFechaHoraEquals(Date fechaHora) throws CitasDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " WHERE FECHA_HORA = ? ORDER BY FECHA_HORA", new Object[] { fechaHora==null ? null : new java.sql.Timestamp( fechaHora.getTime() ) } );
	}

	/** 
	 * Returns all rows from the CITAS table that match the criteria 'ID_NOTARIA = :idNotaria'.
	 */
	public Citas[] findWhereIdNotariaEquals(long idNotaria) throws CitasDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " WHERE ID_NOTARIA = ? ORDER BY ID_NOTARIA", new Object[] {  new Long(idNotaria) } );
	}

	/** 
	 * Returns all rows from the CITAS table that match the criteria 'ID_ESTADO_CITA = :idEstadoCita'.
	 */
	public Citas[] findWhereIdEstadoCitaEquals(long idEstadoCita) throws CitasDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " WHERE ID_ESTADO_CITA = ? ORDER BY ID_ESTADO_CITA", new Object[] {  new Long(idEstadoCita) } );
	}

	/**
	 * Method 'CitasDaoImpl'
	 * 
	 */
	public CitasDaoImpl()
	{
	}

	/**
	 * Method 'CitasDaoImpl'
	 * 
	 * @param userConn
	 */
	public CitasDaoImpl(final java.sql.Connection userConn)
	{
		this.userConn = userConn;
	}

	/** 
	 * Sets the value of maxRows
	 */
	public void setMaxRows(int maxRows)
	{
		this.maxRows = maxRows;
	}

	/** 
	 * Gets the value of maxRows
	 */
	public int getMaxRows()
	{
		return maxRows;
	}

	/**
	 * Method 'getTableName'
	 * 
	 * @return String
	 */
	public String getTableName()
	{
		return "LEX.CITAS";
	}

	/** 
	 * Fetches a single row from the result set
	 */
	protected Citas fetchSingleResult(ResultSet rs) throws SQLException
	{
		if (rs.next()) {
			Citas dto = new Citas();
			populateDto( dto, rs);
			return dto;
		} else {
			return null;
		}
		
	}

	/** 
	 * Fetches multiple rows from the result set
	 */
	protected Citas[] fetchMultiResults(ResultSet rs) throws SQLException
	{
		Collection resultList = new ArrayList();
		while (rs.next()) {
			Citas dto = new Citas();
			populateDto( dto, rs);
			resultList.add( dto );
		}
		
		Citas ret[] = new Citas[ resultList.size() ];
		resultList.toArray( ret );
		return ret;
	}

	/** 
	 * Populates a DTO with data from a ResultSet
	 */
	protected void populateDto(Citas dto, ResultSet rs) throws SQLException
	{
		dto.setIdCita( rs.getLong( COLUMN_ID_CITA ) );
		dto.setFechaHora( rs.getTimestamp(COLUMN_FECHA_HORA ) );
		dto.setIdNotaria( rs.getLong( COLUMN_ID_NOTARIA ) );
		dto.setIdEstadoCita( rs.getLong( COLUMN_ID_ESTADO_CITA ) );
	}

	/** 
	 * Resets the modified attributes in the DTO
	 */
	protected void reset(Citas dto)
	{
	}

	/** 
	 * Returns all rows from the CITAS table that match the specified arbitrary SQL statement
	 */
	public Citas[] findByDynamicSelect(String sql, Object[] sqlParams) throws CitasDaoException
	{
		// declare variables
		final boolean isConnSupplied = (userConn != null);
		Connection conn = null;
		PreparedStatement stmt = null;
		ResultSet rs = null;
		
		try {
			// get the user-specified connection or get a connection from the ResourceManager
			conn = isConnSupplied ? userConn : ResourceManager.getConnection();
		
			// construct the SQL statement
			final String SQL = sql;
		
		
			System.out.println( "Executing " + SQL );
			// prepare statement
			stmt = conn.prepareStatement( SQL );
			stmt.setMaxRows( maxRows );
		
			// bind parameters
			for (int i=0; sqlParams!=null && i<sqlParams.length; i++ ) {
				stmt.setObject( i+1, sqlParams[i] );
			}
		
		
			rs = stmt.executeQuery();
		
			// fetch the results
			return fetchMultiResults(rs);
		}
		catch (Exception _e) {
			_e.printStackTrace();
			throw new CitasDaoException( "Exception: " + _e.getMessage(), _e );
		}
		finally {
			ResourceManager.close(rs);
			ResourceManager.close(stmt);
			if (!isConnSupplied) {
				ResourceManager.close(conn);
			}
		
		}
		
	}

	/** 
	 * Returns all rows from the CITAS table that match the specified arbitrary SQL statement
	 */
	public Citas[] findByDynamicWhere(String sql, Object[] sqlParams) throws CitasDaoException
	{
		// declare variables
		final boolean isConnSupplied = (userConn != null);
		Connection conn = null;
		PreparedStatement stmt = null;
		ResultSet rs = null;
		
		try {
			// get the user-specified connection or get a connection from the ResourceManager
			conn = isConnSupplied ? userConn : ResourceManager.getConnection();
		
			// construct the SQL statement
			final String SQL = SQL_SELECT + " WHERE " + sql;
		
		
			System.out.println( "Executing " + SQL );
			// prepare statement
			stmt = conn.prepareStatement( SQL );
			stmt.setMaxRows( maxRows );
		
			// bind parameters
			for (int i=0; sqlParams!=null && i<sqlParams.length; i++ ) {
				stmt.setObject( i+1, sqlParams[i] );
			}
		
		
			rs = stmt.executeQuery();
		
			// fetch the results
			return fetchMultiResults(rs);
		}
		catch (Exception _e) {
			_e.printStackTrace();
			throw new CitasDaoException( "Exception: " + _e.getMessage(), _e );
		}
		finally {
			ResourceManager.close(rs);
			ResourceManager.close(stmt);
			if (!isConnSupplied) {
				ResourceManager.close(conn);
			}
		
		}
		
	}

}
